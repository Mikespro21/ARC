[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.30"
optimizer = true
optimizer_runs = 200

# Optional: set default RPC endpoint name; you can still pass --rpc-url
# via CLI using ARC_TESTNET_RPC_URL in your shell.
